import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prasa.employeeapp',
  appName: 'EmployeeExpenseApp',
  webDir: 'build'
};

export default config;
